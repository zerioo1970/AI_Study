# Python + Pandas 学习清单（每天 2 小时 · 12 周）

> 面向已有 C# / SQL Server 开发经验、目标是做 A 股表格数据建模的学习者。
> **原则：只学用得上的。宁可窄，不可散。**
>
> 📌 **本文是"学什么"（大纲与验收标准）。
> "今天打开电脑先干什么"请看 → [Python学习详细步骤](Python学习详细步骤.md)**（逐日代码，可直接照敲）

- 创建日期：2026-07-24
- 版本：v1.0

---

## 每天 2 小时怎么分配

| 时长 | 内容 |
|------|------|
| 30 分钟 | 看教程 / 读文档（学新知识） |
| **80 分钟** | **敲代码**（照着做 + 自己改 + 出错调试） |
| 10 分钟 | 记笔记：今天学了什么、踩了什么坑 |

> 🚨 **看与写的比例必须至少 1 : 2。** 只看不写等于没学。
> 这是 55 岁与 25 岁真正的差别所在 —— 不是记忆力，而是**是否愿意反复动手**。

---

## 第 1–2 周：Python 核心语法

### 必学（够用就停）

- 变量与基本类型：`int` / `float` / `str` / `bool`
- 容器：`list`、`dict`、`tuple`、`set` —— **重点 list 和 dict**
- 控制流：`if` / `for` / `while`、`range`、列表推导式 `[x for x in ...]`
- 函数：定义、参数、默认值、返回值
- 字符串：f-string 格式化 `f"{x:.2f}"`、常用方法
- 模块：`import`、`from ... import`
- 异常：`try / except`
- 文件读写：基本操作
- 环境：虚拟环境、`pip install`

### 明确不用学

❌ 类的继承与多态（会用现成类即可）· ❌ 装饰器 · ❌ 异步 `async/await`
❌ 元类 · ❌ GUI · ❌ Web 框架 · ❌ 多线程/多进程（后期真需要再说）

### C# 背景的对照速查（帮你少走弯路）

| C# | Python | 备注 |
|----|--------|------|
| `{ }` 代码块 | **缩进** | 🚨 缩进即语法，必须统一（用 4 空格） |
| 语句末 `;` | 不需要 | |
| `List<T>` | `list` | 但可混装不同类型 |
| `Dictionary<K,V>` | `dict` | |
| `var` / 强类型 | 动态类型 | 变量可随时改类型（自由但易出错） |
| `for(int i=0;...)` | `for i in range(n)` | |
| `foreach (var x in c)` | `for x in c` | |
| `null` | `None` | |
| `string.Format` | f-string | `f"{name} 收盘 {price:.2f}"` |
| `//` 注释 | `#` | |

> 💡 你的 SQL 经验会在 Pandas 阶段变成巨大优势，Python 语法这一段只是过路费，快速通过即可。

### 第 2 周末自测（能独立写出来才算过）

- [ ] 写一个函数，输入一个价格列表，返回每日涨跌幅列表
- [ ] 写一个函数，输入价格列表和窗口 n，返回 n 日移动平均列表（**先用纯 Python 手写一遍**，
      后面用 Pandas 一行搞定时，你才会真正体会到 Pandas 的价值）
- [ ] 读一个 csv 文件，逐行解析并打印

---

## 第 3–6 周：Pandas（重中之重）⭐

**这是整个学习计划中最重要的部分。** 后面所有工作都建立在这上面。

### 3.1 基础对象与操作（第 3 周）

- `Series` 与 `DataFrame` 的区别
- `pd.read_csv()` / `to_csv()`
- 查看数据：`head()`、`tail()`、`info()`、`describe()`、`shape`、`dtypes`
- 选取：**`loc`（按标签）vs `iloc`（按位置）** —— 必须彻底搞清楚
- 布尔筛选：`df[df["close"] > 100]`、多条件 `&` `|`（**注意要加括号**）
- 新增/修改列
- 缺失值：`isna()`、`dropna()`、`fillna()`、`ffill()`

### 3.2 时间序列操作（第 4–5 周）⭐⭐⭐ 命脉所在

**80% 精力放这里。** 这几个函数就是你未来所有特征工程的全部工具：

| 函数 | 作用 | 在本项目中的用途 |
|------|------|-----------------|
| `pd.to_datetime()` | 转日期类型 | 数据入口 |
| `set_index()` / `sort_index()` | 设日期索引并排序 | 时序前提 |
| **`shift(n)`** | 整列上下平移 | 🚨 **防泄漏的核心武器**，取"昨天的值" |
| **`rolling(n)`** | 滚动窗口 | `.mean()` 均线、`.std()` 波动率、`.max()/.min()` 区间 |
| **`diff(n)`** | 差分 | 变化量、**二阶变化**（斜率的变化） |
| **`pct_change(n)`** | 百分比变化 | 收益率 |
| `ewm()` | 指数加权 | EMA 均线 |
| `expanding()` | 扩展窗口 | 累计统计（注意会用到全历史） |
| `resample()` | 重采样 | 日→周（⚠️ 股票须用真实交易日历，不可直接按自然日） |

**必须练到条件反射的三个组合**：

```python
# 1. 5 日均线（只用到今天及之前，本身不含未来）
df["ma5"] = df["close"].rolling(5).mean()

# 2. 昨天的 5 日均线（防泄漏的标准写法）
df["ma5_prev"] = df["close"].rolling(5).mean().shift(1)

# 3. 二阶变化：趋势在加速还是减速 ⭐ 本项目核心特征
df["ret5"] = df["close"].pct_change(5)          # 5 日收益
df["ret5_chg"] = df["ret5"].diff(5)             # 5 日收益的变化 = 动量的变化
```

> 💡 第 3 个就是「涨得越来越慢」的数学表达。**理解它，你就理解了本项目的核心思想。**

### 3.3 分组与合并（第 6 周）

你的 SQL 背景在这里直接变现：

| SQL | Pandas |
|-----|--------|
| `GROUP BY` | `groupby()` |
| `JOIN` | `merge()` |
| `UNION ALL` | `concat()` |
| 行转列 | `pivot()` / `pivot_table()` |
| 窗口函数 `OVER` | `groupby().transform()` / `rolling()` |

多股票面板数据时的关键写法：
```python
# 按股票分组后再做时序运算，绝不可跨股票串味
df["ma5"] = df.groupby("code")["close"].transform(lambda s: s.rolling(5).mean())
```

### 3.5 关于 NumPy：不单独学，穿插学（半天到 1 天）

**结论：NumPy 不作为独立学习阶段。** Pandas 本身建在 NumPy 之上，
写 `df["close"].rolling(5).mean()` 时底层就是 NumPy 在算 —— 你一直在用它，
但不需要直接面对它。

#### 实际会用到的只有七个

| 用法 | 干什么 | 用在哪 |
|------|--------|--------|
| **`np.where`** | 条件赋值（≈ SQL 的 `CASE WHEN`） | ⭐ **做三分类标签，必用** |
| `np.nan` | 缺失值 | 到处 |
| `np.sqrt` | 开方 | 波动率换算（√N 阈值） |
| `np.log` | 对数 | 对数收益率 |
| `np.sign` | 取符号 | 判断方向 |
| `np.clip` | 截断极值 | 处理异常值 |
| `.to_numpy()` | 转成数组 | 喂给 LightGBM |

#### 本项目会真实写出的代码（第 16 周左右）

```python
import numpy as np

# 未来 5 日收益率（标签可以用未来信息）
fwd_ret = df["close"].pct_change(5).shift(-5)

# 波动率自适应阈值：近 20 日日波动率 × √5
vol = df["close"].pct_change().rolling(20).std()
thr = 1.0 * vol * np.sqrt(5)

# 三分类（np.where 嵌套，相当于 SQL 的 CASE WHEN）
df["label"] = np.where(fwd_ret >  thr,  1,      # 上升
                np.where(fwd_ret < -thr, -1,     # 下降
                                          0))    # 盘整
```

> 整段代码里 NumPy 只出现两次（`np.sqrt`、`np.where`）。**这就是所需的 NumPy 水平。**
> 对 SQL 背景者：`np.where(条件, 真值, 假值)` 就是 `CASE WHEN ... THEN ... ELSE ... END`。

#### 明确不学

❌ 多维 `ndarray`（2 维以上）· ❌ `reshape` / `transpose` / axis 复杂操作
❌ **广播机制细节规则** · ❌ `np.linalg`（矩阵求逆、特征值） · ❌ 随机数高级用法
❌ 结构化数组 · ❌ `np.einsum`

> 这些是给做图像、深度学习、数值计算的人用的。做表格数据一辈子用不上。

#### ⭐ 唯一真正重要的概念：向量化思维

```python
# ❌ 循环写法（C# 习惯，几万行会慢到无法接受）
returns = []
for i in range(1, len(prices)):
    returns.append(prices[i] / prices[i-1] - 1)

# ✅ 向量化写法（一行，快几百倍）
returns = df["close"].pct_change()
```

> 🚨 **来自 C# 的人最容易犯的错，就是习惯性写 `for` 循环。**
> 在 Pandas / NumPy 里，**看到自己在用 for 循环遍历数据行，就该停下来问：有没有向量化写法？**
> 答案通常是"有"。

这不只是风格问题，而是**本项目的实际约束**：方案 2 要在 2 分钟内扫描 5000 只股票，
**只有向量化才做得到**。

**时间安排**：穿插在第 3–6 周 Pandas 阶段，遇到就查，总投入半天到 1 天。
优先级远低于 `shift` / `rolling` / `diff`。**省下的时间全部投给 Pandas 时序操作。**

---

### 3.4 Pandas 五个必踩的坑（提前知道能省几十小时）

1. **`SettingWithCopyWarning`** —— 链式索引赋值 `df[a][b] = x` 无效。
   正确写法：`df.loc[条件, "列名"] = x`
2. **链式索引** —— `df["a"]["b"]` 有时是副本有时是视图，行为不确定。统一用 `.loc`
3. **`inplace=True`** —— 官方已不推荐，容易出意外。统一用赋值：`df = df.dropna()`
4. **索引自动对齐** —— 两个 Series 相加会按索引对齐，索引不同会产生 `NaN`。
   这是 Pandas 最容易困惑初学者的行为
5. **`rolling` 的 `min_periods`** —— 默认窗口不满时返回 `NaN`。
   ⚠️ **不要为了消掉 NaN 而设 `min_periods=1`**，那会让早期数据用不足的窗口计算，引入偏差

---

## 第 7–8 周：数据可视化与工具链

### Matplotlib（只学够用的）
- 折线图、多子图、双 Y 轴
- 标记特定日期（用于标出转折点）
- 中文字体配置（常见坑，需单独处理）

> 💡 **画图是最重要的调试手段。** 把模型标出的转折点画在 K 线图上，
> 一眼就能看出是"真发现了规律"还是"在瞎标"。这比任何指标都直观。

### 工具链
- 环境管理：conda / miniconda 或 uv（选一个即可）
- **Jupyter Notebook** —— 数据分析必备，支持逐块执行、随时看中间结果
- VS Code 基础
- Git 基础：`add` / `commit` / `push`（本仓库已建好，可直接练手）

---

## 第 9–12 周：A 股数据实战（学以致用）

这一阶段开始接触真实数据，也是学习动力的来源。

- [ ] 用 **akshare** 下载沪深300 指数日线（20 年）
- [ ] **搞清前复权 / 后复权 / 不复权的区别**，确认自己用的是后复权
- [ ] 处理交易日历，确认无缺失日、无重复日
- [ ] 画出 20 年走势图，与行情软件核对，**确认数据正确**
- [ ] 计算并画出：MA5 / MA20 / MA60、20 日波动率、日收益率分布
- [ ] 手动标出你**自己认为**的几个历史转折点，画在图上
      （💡 这一步极有价值：会让你意识到"转折点"这个定义本身有多模糊）
- [ ] 用 `shift` 构造 10 个特征，并写断言验证无未来函数
- [ ] 写一个每日自动下载并归档数据的脚本（**开始积累数据**）

---

## 12 周后的验收标准

能独立完成以下事项，即可进入建模阶段：

- [ ] 看到一个 Pandas 报错，知道大致该往哪查
- [ ] 能用一行代码算出任意窗口的滚动统计量
- [ ] 能解释 `shift(1)` 为什么是防泄漏的关键
- [ ] 能说出 `df.loc` 和 `df.iloc` 的区别
- [ ] 能画出带均线的价格图并标注特定日期
- [ ] 有一份干净、可复现、经过核对的沪深300 日线数据

---

## 推荐资源类型（不指定具体教程，选一个坚持到底）

| 类型 | 说明 |
|------|------|
| Pandas 官方文档 | 特别是 *10 minutes to pandas* 与 Time Series 章节，**质量最高** |
| 任一系统性 Python 入门课 | 选一个就坚持完，**不要同时开三门** |
| Kiro / AI 助手 | 遇到不懂的代码逐行问，效率最高的学习方式 |

> ⚠️ **最大的风险是"教程地狱"** —— 看了很多课但从没独立写过一个完整脚本。
> 判断标准很简单：**你今天敲的代码超过 50 行了吗？**

---

## 学习期间与 Kiro 的配合方式

1. **每周固定时间**同步一次进度，我更新进度表和讨论记录
2. 遇到卡点随时问 —— 贴代码和报错，比自己查快得多
3. 我会**持续更新 [技术雷达](技术雷达.md)**，跟踪最新 AI 成果能否用在本目标上
4. 你学到某个阶段，我就把对应的实战代码写出来，你**读懂并修改**
   —— 读懂别人的代码是最快的进步方式
