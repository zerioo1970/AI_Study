# AI 学习路线图（个人定制 · 活文档）

> **目标**：用 AI 对**表格化数据**做趋势预测，核心难点是**判断当下是否为趋势的转折点**。
> 本文档为长期迭代文档，每次讨论后更新。变更历史见 [讨论记录](讨论记录.md)。

- 创建日期：2026-07-24
- 最近更新：2026-07-24
- 当前版本：**v1.0**

---

## 0. 个人档案（决定路线的前提）

| 项目 | 现状 |
|------|------|
| 年龄 | 55 岁 |
| 既有技术背景 | C# ASP.NET WebForm + SQL Server，企业系统开发，SQL 与业务理解扎实 |
| Python 水平 | 入门（会一点） |
| 数学基础 | 大学数学已遗忘 |
| 定位 | **应用开发**（不做算法研究、不发论文） |
| 每日可投入 | **最多 2 小时** |

**核心结论**：年龄不是障碍；数学 90% 不需要补；2 小时/天足够。
关键是**不要走错方向、不要铺太宽**。

---

## 1. 目标拆解与难度认知

| 目标 | 难度 | 说明 |
|------|------|------|
| 预测趋势（延续） | ⭐⭐ | 有成熟方法，相对容易 |
| **判断当下是转折点** | ⭐⭐⭐⭐⭐ | 时间序列中最难的问题之一 |

**根本矛盾**：转折点只有事后才能确认。当下看到的"疑似转折"与噪音波动在数据上高度相似。

→ 这个矛盾无法消除，只能在 **及时性 ↔ 准确性** 之间做权衡。
想清楚这一点，就已经超过 90% 的初学者。

---

## 2. 技术地图（照着这些关键词去搜、去学）

| 需求 | 技术术语 | 关键工具 |
|------|---------|---------|
| 表格数据建模 | Tabular ML / GBDT | **LightGBM** |
| 时序特征构造 | Lag / Rolling Features | Pandas |
| 趋势预测 | Time Series Forecasting | LightGBM，Prophet（仅作基线） |
| **转折点检测** | **Change Point Detection**<br>**Regime Shift Detection** | **ruptures**、CUSUM、HMM |
| 找历史高低点 | Local Extrema / Zigzag | `scipy.signal.argrelextrema` |
| 正确验证方式 | Walk-Forward Validation | `sklearn.model_selection.TimeSeriesSplit` |
| 模型解释 | Explainability | **SHAP** |
| 自动调参 | HPO | Optuna |

---

## 3. 只需要这些数学（其余全部跳过）

**需要**（初中水平即可）：
- 平均值、标准差 —— 衡量波动大小
- 百分比变化、差分 —— 变化了多少
- 相关性 —— 两个变量是否同向
- 概率直觉 —— 输出 0.7 理解为"七成可能"

**不需要**：线性代数、微积分、矩阵求导、损失函数推导。

> LightGBM 可理解为"一个很聪明的**自动 if-else 生成器**"。

---

## 4. 核心：把"转折点"变成机器学习问题

这一步比调参重要 100 倍。三种做法，由易到难：

### 做法 A —— 回归（起步，先验证流程）
```
标签 = 未来 N 期的变化率
```
输出数值。简单，但对转折不敏感。

### 做法 B —— 三分类（推荐正式起点）
```
未来 N 期变化 > +阈值  → 上升
未来 N 期变化 < -阈值  → 下降
其余                   → 盘整
```
"阈值 + 盘整类"是关键：过滤噪音，让模型只在有把握时表态。

### 做法 C —— 转折点标注（最终目标）
```python
from scipy.signal import argrelextrema
# 事后标出历史局部极大/极小值 = 真实转折点
# 标签 = 当前时点是否处于转折点附近（±k 期内）
```

注意事项：
- 转折点极稀少（约 2~3%）→ **极度不平衡分类**，准确率无意义，看**精确率 / 召回率**
- 标签定义可用未来信息；**特征中绝不可含未来信息**

---

## 5. 六个致命陷阱（时间序列专属，请反复回看）

### 1. 数据泄漏 / 未来函数 —— 头号杀手
任何特征只能用当前及之前的数据。用 `.rolling()` 而非全局 `.mean()`，注意 `shift(1)`。

### 2. 随机划分训练集 —— 绝对禁止
```python
# ❌ train_test_split(X, y, shuffle=True)
# ✅ TimeSeriesSplit / 按时间切分
```
随机划分等于"用明天预测昨天"，结果好得离谱，上线全崩。

### 3. 没有基线对比 → 无法判断好坏
必须对比：
1. 随机漫步（明天 = 今天）
2. 趋势延续
3. 移动平均交叉

**打不过这三个，模型即无用。** 这条能省下无数无用功。

### 4. 反复调参调出的"假好结果"
在同一测试集试 200 组参数选最好的 → 那是**运气**。
留一段**从头到尾没碰过**的最终数据，全部工作完成后只测一次。

### 5. 忽略确认延迟
必须记录：**信号发出时距真实转折已过多少期。**
此指标比准确率更能说明系统价值。

### 6. 概念漂移
历史规律会失效。任何"过去 10 年回测很准"都要追问：**最近 1 年表现如何？**

---

## 6. 明确不学的内容（2h/天，时间宝贵）

- ❌ 深度学习（CNN / RNN / LSTM / Transformer）—— 表格与中小时序上通常打不过 LightGBM
- ❌ 数学公式推导、反向传播
- ❌ 大模型微调、训练自己的 LLM
- ❌ 图像、语音、NLP
- ❌ ARIMA 数学理论（平稳性检验、ACF/PACF）—— 知概念即可
- ❌ 各种花哨新框架

> **唯一路线：Pandas + LightGBM + 正确验证 + 特征工程。窄而深。**

---

## 7. 六个月计划（每天 2 小时）

### 第 1–3 周：Pandas 打通
- `read_csv`、索引切片、`groupby`、`merge`（SQL 背景上手快）
- **重点**：`DatetimeIndex`、`resample()`、`rolling()`、`shift()`、`diff()`、`pct_change()`
- 练习：取一份熟悉的历史数据，画走势图，算移动平均与波动率

> ⚠️ 80% 精力放在 `rolling` 和 `shift` —— 后续特征工程的命脉。

### 第 4–6 周：机器学习最小必要概念
- 训练 / 验证 / 测试集（**按时间切**）
- 过拟合是什么、为何可怕
- 分类 vs 回归
- 评估指标：**精确率、召回率、AUC**（理解"宁可少报还是宁可多报"）
- ⛔ 不碰数学推导

### 第 7–10 周：LightGBM 上手 + 第一个闭环
- 跑通 `LGBMClassifier` / `LGBMRegressor`
- 掌握 `early_stopping`、`num_leaves`、`learning_rate`
- 完成闭环：数据 → 特征 → 训练 → 时序验证 → **与基线对比**
- 目标不是准，是**流程正确**

### 第 11–16 周：特征工程（投入最多时间，决定成败）

把时间序列"摊平"成表格：

| 特征类型 | 例子 |
|---------|------|
| 滞后特征 | 前 1/2/3/5/10 期的值 |
| 滚动统计 | 5/10/20 期均值、标准差、最大最小 |
| 变化率 | 1 / 5 / 20 期涨跌幅 |
| 位置特征 | 在过去 20 期中的百分位、Z-score |
| 斜率 / 加速度 | 趋势方向 + **趋势的变化**（转折关键） |
| 偏离度 | 当前值 / 移动平均 − 1 |
| 波动状态 | 波动率是否在放大 |
| 时间特征 | 星期、月份、季节性 |

> 💡 **核心洞察：转折信号藏在"二阶变化"里** —— 不是"在涨"，而是"**涨得越来越慢**"。
> 斜率变化、动量衰减、波动率突变，远比原始值重要。

### 第 17–20 周：专攻转折点检测
- **`ruptures`** 库（开箱可用的 change point detection）
- CUSUM（累积和检测偏移）、HMM（状态切换 / regime）
- `argrelextrema` 标注历史拐点，训练分类模型
- 不平衡处理：`scale_pos_weight`、调整判定阈值
- **SHAP** 分析模型究竟依据什么判断转折 → 产生业务洞察

### 第 21–26 周：验证、加固、落地
- Walk-forward 滚动回测，模拟真实使用
- 分时期检验稳定性（不同年份 / 不同环境）
- 输出**概率 + 置信度**，而非硬性"是 / 否"
- 做成日常运行脚本 + 可视化报告
- 可选：Streamlit 轻量界面（半天可学）
- 可选部署形态：Python 训练并提供 API → 由现有 C#/.NET 系统调用（复用既有技能）

---

## 8. 两条心法

### 1. 追求"有用"，不追求"神准"
转折点检测达到 60% 精确率、提前 2–3 期给信号，即**非常有价值**。
号称 90% 准的，基本都是数据泄漏。降低期待反而更容易成功。

### 2. 让模型做"报警器"，不做"决策者"
输出"当前转折风险 = 0.73"，作为**辅助信号**，与自身业务经验结合。
比追求全自动预测靠谱得多，也更能发挥个人经验优势（你有经验，模型有算力）。

---

## 9. 待补充信息（影响方案精度）

- [ ] 数据所属领域？（销售量 / 产量 / 价格 / 业务指标 …）
- [ ] 时间粒度？（日 / 周 / 月）
- [ ] 历史记录条数大致多少？
- [ ] 单一序列还是多序列（多产品 / 多地区）？
- [ ] 转折判断可接受的延迟是几期？

> 数据量尤其关键：若仅几十~上百条，LightGBM 易过拟合，策略需整体调整。

---

## 10. 进度追踪

| 阶段 | 状态 | 起止 | 备注 |
|------|------|------|------|
| 第 1–3 周 Pandas 时序基础 | ⬜ 未开始 | | |
| 第 4–6 周 ML 最小概念 | ⬜ 未开始 | | |
| 第 7–10 周 LightGBM 闭环 | ⬜ 未开始 | | |
| 第 11–16 周 特征工程 | ⬜ 未开始 | | |
| 第 17–20 周 转折点检测 | ⬜ 未开始 | | |
| 第 21–26 周 验证与落地 | ⬜ 未开始 | | |

图例：⬜ 未开始 / 🟨 进行中 / ✅ 已完成 / ⏸ 暂停

---

## 11. 备选下一步

- **A.** 可运行代码框架：自动生成时序特征 → LightGBM 三分类 → 时序验证 → 基线对比 ← **推荐先做**
- **B.** 转折点标注 + 检测示例（`argrelextrema` + `ruptures` + LightGBM）
- **C.** 六个月计划细化为每周任务清单
- **D.** Pandas 时序三件套速成（`shift` / `rolling` / `diff`）
